version: 2
jobs:
    build:
        docker:
            - image: byuoitav/build-env:feature/couch-rework

        working_directory: /go/src/github.com/byuoitav/touchpanel-ui-microservice

        steps:
            - checkout
            - setup_remote_docker
            - run: make deps
            - run: make build
            - run: make docker
